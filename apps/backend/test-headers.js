const axios = require('axios');

async function test() {
  // Use public menu to find restaurant ID
  // Wait, I need a token to test the authenticated endpoints.
  console.log("Need a token to test properly.");
}
test();
